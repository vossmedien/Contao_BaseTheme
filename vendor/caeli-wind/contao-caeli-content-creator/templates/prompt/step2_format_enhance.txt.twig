**CONTENT-FORMATIERUNG: HTML-PHASE**

**THEMA:** {{ topic }}{% if year is not empty %} ({{ year }}){% endif %}
{% if targetAudience %}**ZIELGRUPPE:** {{ targetAudience }} - RICHTE TONALITÄT, BEISPIELE UND ANSPRACHE EXAKT AUF DIESE GRUPPE AUS!{% endif %}

**ROHTEXT:**
---
{{ raw_content }}
---

{% if additionalInstructions %}**PROJEKT-KONTEXT & WICHTIGE ANFORDERUNGEN:**
{{ additionalInstructions }}
{% endif %}

**FORMATIERUNGS-ANFORDERUNGEN:**

{% if targetAudience %}**ZIELGRUPPEN-OPTIMIERUNG:**
- Alle Beispiele, Metaphern und Sprache müssen auf "{{ targetAudience }}" zugeschnitten sein
- Verwende branchenspezifische Begriffe und relevante Problemstellungen
- Vermeide abstrakte Konzepte - fokussiere auf praktischen Nutzen
- Ansprache sollte die Lebenswelt von "{{ targetAudience }}" widerspiegeln
{% endif %}

**1. HTML-STRUKTUR:**
- Formatiere mit `<h2>`, `<h3>` (KEINE H1!), `<p>`, `<strong>`, `<em>`
- SEO-Keywords in Überschriften: {{ topic }}{% if year is not empty %}, {{ year }}{% endif %}
- Leserfreundliche Absätze (3-5 Sätze)
- **WICHTIG:** Nur alle 3-4 Absätze eine neue H3-Überschrift setzen, nicht nach jedem Absatz!

**2. BOOTSTRAP-ELEMENTE (2-3 passende auswählen):**
- Info-Card: `<div class="card mb-4"><div class="card-body"><h5 class="card-title">Titel</h5><p class="card-text">Text</p></div></div>`
- Statistik-Highlight: `<div class="card text-center mb-4"><div class="card-body"><p class="display-4 text-primary">Zahl</p><p class="card-text">Erklärung</p></div></div>`
- Info-Alert: `<div class="alert alert-info mb-4">Wichtige Information</div>`
- Erfolgs-Tipp: `<div class="alert alert-success mb-4">Positiver Aspekt</div>`
- Warnung: `<div class="alert alert-warning mb-4">Hinweis</div>`
- Tabelle: `<table class="table table-striped mb-4"><thead><tr><th>Spalte</th></tr></thead><tbody><tr><td>Daten</td></tr></tbody></table>`

**3. WIDGETS (2-3 thematisch passend platzieren):**
{% set widgets = {
    4: 'Newsletteranmeldung',
    5: 'Grundeigentümer-Terminvereinbarung', 
    6: 'Flächencheck ohne PLZ',
    7: 'Flächencheck mit PLZ',
    18: 'Pachteinnahmen-Rechner'
} %}
Verfügbare Widgets für relevante Platzierung:
{% for id, name in widgets %}
- `<div>{{insert_node::{{ id }}}}</div>` ({{ name }})
{% endfor %}
{% if targetAudience %}**Widget-Auswahl:** Wähle Widgets die für "{{ targetAudience }}" am relevantesten sind!{% endif %}

**4. LINKS & QUELLEN:**
{% if include_sources %}**WICHTIG:** Wenn Fakten, Statistiken oder Studien erwähnt werden, verlinke DIREKT zu den konkreten Berichten/Studien:
- `(Quelle: <a href="https://konkrete-studie-url.de/bericht-2024.pdf" target="_blank">Studienname, Jahr</a>)`
- Nutze dein Wissen um ECHTE URLs zu aktuellen Berichten zu finden
- KEINE separate Quellenliste am Ende - Links gehören direkt in den Text{% else %}Keine Quellenlinks erforderlich{% endif %}
- Andere externe Links: `<a href="URL"{% if add_target_blank %} target="_blank"{% endif %}>Text</a>`

**AUSGABEFORMAT (exakt einhalten):**
```
Prägnanter SEO-Titel (OHNE Markdown-Zeichen wie ```)|||+++|||Kurzer Teaser-Text{% if targetAudience %} für {{ targetAudience }}{% endif %}|||+++|||<h2>Erste Überschrift</h2><p>Formatierter HTML-Inhalt...</p>|||+++|||Keyword1, Keyword2, Keyword3
```

**WICHTIG:** 
- Titel darf KEINE Markdown-Zeichen (```, **) enthalten
- H3-Überschriften sparsam verwenden (max. 4-5 im ganzen Artikel)
- 2-3 Widgets strategisch verteilen
- 2-3 Bootstrap-Elemente einbauen
{% if targetAudience %}- ALLE Inhalte müssen auf "{{ targetAudience }}" zugeschnitten sein{% endif %}

**VERBOTEN:** H1-Tags, Markdown-Zeichen im Titel, zu viele H3-Überschriften, zielgruppenfremde Beispiele





