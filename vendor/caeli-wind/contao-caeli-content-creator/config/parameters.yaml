#config/parameters.yaml
parameters:
    # caeli.content_creator.min_words_default: 1000 # Removed, value comes from DCA record
    caeli.content_creator.api_timeout: 300 # Default API timeout in seconds, erhöht auf 300s
    caeli.content_creator.api_max_tokens: 64000 # Increase max output tokens again

