<div class="col" data-animation="animate__fadeIn">
    <article class="auction-item {{ class }}" id="{{ cssID }}">
        <div class="card" style="--bs-card-bg: var(--bs-primary); --bs-card-cap-bg: var(--bs-primary); --bs-card-color: var(--bs-body-color-inverse); --bs-card-title-color: var(--bs-card-color)">
            <div class="card-header">
                <h3 class="card-title h5 mb-0">
                    <span class="d-block">{{ 'auction_listing.card.title_prefix'|trans }}</span>
                    <span class="fw-bold">{{ raw_data.state }}</span>
                </h3>
            </div>
            <div class="card-img">
                {# Bild #}
                {% set image_path = auction.blurred_picture_path|default(auction.picture_path|default(null)) %}
                {% if image_path %}
                    <div class="card-img-top auction-image-wrapper">
                        <img src="{{ image_path }}"
                             alt="{{ 'auction_listing.card.image_alt_prefix'|trans }} {{ auction.id }}" class="auction-image img-fluid">

                        {% if raw_data.countDown is defined %}
                            {% set days_countdown = (raw_data.countDown / 86400)|round(0, 'ceil') %}
                            {% if days_countdown > 0 %}
                                <span class="badge bg-primary text-secondary countdown-badge">{{ 'auction_listing.card.countdown_prefix'|trans }} {{ days_countdown }} {{ 'auction_listing.card.countdown_suffix_days'|trans }}</span>
                            {% endif %}
                        {% endif %}
                    </div>
                {% endif %}
            </div>
            <div class="card-body">
                <p>
                    #TEXT#<br>
                    ENTEGA gewinnt die erste digitale Flächenausschreibung in Deutschland. Im Büdinger Wald, Nordhessen
                    entstehen auf 250 ha bald 10 WEAs, die mit 60 MW 45.000 Haushalte mit klimaneutralem Strom versorgen
                    können.
                </p>
            </div>
        </div>
    </article>
</div>
