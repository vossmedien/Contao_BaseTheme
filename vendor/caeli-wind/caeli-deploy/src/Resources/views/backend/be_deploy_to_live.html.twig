{% extends "@ContaoCore/Backend/be_page.html.twig" %}

{% block headline %}
    Deploy to LIVE-System
{% endblock %}

{% block main %}
<div class="be_deploy_to_live" style="padding:15px">
    <h2>Diese Version von Contao auf das LIVE-System übertragen.</h2>
    <br>
    <p>
        Mit der Eingabe <b>"ausrollen"</b> <br>
        wird das Contao so wie es unter <u>staging.caeli-wind.de</u> erreichbar ist auf <u>caeli-wind.de</u> ausgerollt.
        <br><br>
        Mit der Eingabe <b>"rollback"</b> <br>
        wird die letzt Version wieder auf <u>caeli-wind.de</u> zurückgerollt.

        <br><br><br>
        <span style="font-style: italic">
            <b>ACHTUNG:</b><br>
            Es gibt nur <b>EINE</b> letzte Version. <br>Sollte also zweimal hintereinander die eingabe <b>"ausrollen"</b> gemacht werden, sind die Stände im Backup identisch zum LIVE und es kann keine Version mehr zurück gerollt werden.
            <br><br><br>
        </span>
    </p>
    <div style="margin-top: 10px">
        <form action="{{ path('caeli_deploy') }}" method="POST">
            <input type="text" name="ausrollen">
            <input type="hidden" name="TL_SUBMIT" value="deploy">
            <input type="hidden" name="REQUEST_TOKEN" value="{{ csrf_token('contao_csrf_token') }}">
            <input type="submit" value="Absenden" name="deploy" />
        </form>
        {% if message %}
            <div class="message">{{ message }}</div>
        {% endif %}
    </div>
</div>
{% endblock %} 